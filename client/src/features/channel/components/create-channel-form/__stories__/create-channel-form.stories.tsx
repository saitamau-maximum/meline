import { Meta } from "@storybook/react";
import { CreateChannelForm } from "..";

const meta = {
  title: "Features/Channel/Components/CreateChannelForm",
} satisfies Meta;

export default meta;

export const Overview = () => <CreateChannelForm />;
